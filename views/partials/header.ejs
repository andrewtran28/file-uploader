<!DOCTYPE html>
<html lang="en">
  <body>
    <header id="header">
      <a href="/"><img id="logo" src="/logo.png"></a>
      <div class="header-right">
        <% if (currentUser) { %>
        <span
          >Welcome,
          <a href="/folder">
            <%= currentUser.username %>
          </a>
        </span>
        <span class="logout"><a href="/logout">Log Out<img class="icon" src="/icons/logout.svg"></a></span>
        <% } else { %>
        <a href="/login">
          <button id="sign-in" class="btn-green">Sign-In</button>
        </a>
        <span class="logout">Don't have an account? <a href="/signup">Sign-Up</a>!</span>
        <% } %>
      </div>
    </header>
  </body>
</html>
